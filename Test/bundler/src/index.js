const message = require("./message");
console.log(message);

/// To compile the this, make sure you have installed webpack to the exect version and run ->   export NODE_OPTIONS=--openssl-legacy-provider  : in terminal
